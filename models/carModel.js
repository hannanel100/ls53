const carModel = (id, name, price, monthly, currency, doors, seats, image) => {
    id,
        name,
        price,
        monthly,
        currency,
        doors,
        seats,
        image
}

module.exports = carModel;